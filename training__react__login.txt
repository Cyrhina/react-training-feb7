- create user using API
- redirect to checkinbox
--> go to store context (store action, store contenxt Reducer)
   - create isSignup


create useEffect on createAccount 
 React.useEffect(() => {
    dispatch(setIsSignup(true));
  }, []);


import useNavigate
  const navigate = useNavigate();

use navigate, add it to fetchData

on fetchData.js add

 if (store.isSignup) {
    dispatch(setIsSignup(false));
    navigate(`${devNavUrl}/checkinbox`);
 }

import devNavUrl, setIsSignup



update the notfication - verify email html-new-account.php
goto new-account.php 
goto create-user uncomment Response::sendEmail


Go to create password

para mgsend nng email gawa nng context para sa redirection

